# ROS 2 Gazebo Desktop - Remote Ubuntu Server Configuration
# =============================================================
# Use this configuration when running Docker on a remote Ubuntu server
# and displaying GUI on your Windows host via X11 forwarding.

# DISPLAY Configuration for Remote X11
# =====================================
# Set this to your WINDOWS HOST IP address (where VcXsrv is running)
# Find your Windows IP with 'ipconfig' in PowerShell
# Example: If your Windows IP is 192.168.1.100, use:
DISPLAY=192.168.1.100:0.0

# For SSH X11 forwarding, leave this empty or use the SSH-provided value
# The entrypoint will use the environment's DISPLAY if set
# DISPLAY=

# User Configuration
USER_UID=1000
USER_GID=1000
USERNAME=rosuser

# ROS 2 Configuration
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# Container Configuration
CONTAINER_NAME=ros2_gazebo_remote

# Workspace Path (on the Ubuntu server)
HOST_WORKSPACE=./ros2_ws

# Network Configuration
# For remote server, 'host' mode uses server's network
# This is good for ROS 2 discovery within the server's network
NETWORK_MODE=host

# GPU Acceleration
# Set to 'true' if Ubuntu server has NVIDIA GPU
ENABLE_GPU=false

# Gazebo Configuration
GZ_SIM_RESOURCE_PATH=/home/rosuser/ros2_ws/src/models
GZ_PARTITION=gazebo
GZ_IP=127.0.0.1
