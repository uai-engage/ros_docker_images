# Environment variables for PX4/ArduPilot Development Setup
# Copy this to .env and customize for your setup

# ============================================
# User Configuration
# ============================================
USERNAME=rosuser
USER_UID=1000
USER_GID=1000

# ============================================
# Container Names
# ============================================
CONTAINER_NAME_DESKTOP=ros2_ground_station
CONTAINER_NAME_COMPANION=ros2_companion

# ============================================
# VNC Configuration (Ground Station)
# ============================================
VNC_PASSWORD=rospassword
VNC_RESOLUTION=1920x1080
VNC_DEPTH=24

# ============================================
# ROS 2 Configuration (Shared)
# ============================================
# Must be the same across all containers for DDS discovery
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# ============================================
# Workspace Directories
# ============================================
# Desktop workspace (for custom packages, models)
HOST_WORKSPACE_DESKTOP=./ros2_ws

# Companion workspace (for custom FC interface packages)
HOST_WORKSPACE_COMPANION=./companion_ws

# ============================================
# Gazebo Configuration (Ground Station)
# ============================================
GZ_SIM_RESOURCE_PATH=/home/rosuser/ros2_ws/src/models
GZ_PARTITION=gazebo

# ============================================
# Flight Controller Configuration (Companion)
# ============================================
# PX4 micro-ROS agent settings
XRCE_DDS_PORT=8888

# MAVROS FCU connection (for ArduPilot)
# Options:
#   - Serial: /dev/ttyUSB0:921600
#   - UDP: udp://:14550@
#   - TCP: tcp://192.168.1.100:5760
FCU_URL=udp://:14550@

# ============================================
# Usage Notes
# ============================================
# 1. Ground Station Access:
#    - VNC: localhost:5901 (or server-ip:5901)
#    - noVNC: http://localhost:6080/vnc.html
#
# 2. PX4 Connection:
#    - Set PX4 parameter: XRCE_DDS_0_CFG = 2 (UDP)
#    - Set PX4 parameter: XRCE_DDS_PRT = 8888
#    - Companion will auto-forward to ground station
#
# 3. ArduPilot Connection:
#    - Set FCU_URL to match your connection type
#    - MAVROS2 will bridge MAVLink to ROS 2 topics
#
# 4. ROS 2 Communication:
#    - Both containers share ROS_DOMAIN_ID
#    - Topics automatically discovered via DDS
#    - Use 'ros2 topic list' to see available topics
