# micro-ROS Agent Companion Computer Configuration
# Copy this file to .env and customize for your setup
#
# Usage:
#   cp .env.companion.example .env
#   nano .env  # Edit configuration
#   docker compose -f docker-compose.companion.yml up -d

# ==========================================
# ROS 2 CONFIGURATION
# ==========================================

# ROS Domain ID (must match ground station)
# Default: 0
ROS_DOMAIN_ID=0

# Ground station IP address
# Set this to your ground station's IP address
# This is important for ROS 2 discovery over network
# Default: 192.168.1.100
GROUND_STATION_IP=192.168.1.100

# ==========================================
# CONNECTION MODE CONFIGURATION
# ==========================================

# Connection mode: udp4, tcp4, serial
# - udp4: UDP IPv4 (best for SITL testing)
# - tcp4: TCP IPv4 (reliable network connection)
# - serial: Serial/USB connection (for real hardware)
# Default: udp4
CONNECTION_MODE=udp4

# ==========================================
# UDP/TCP CONFIGURATION (for udp4/tcp4 modes)
# ==========================================

# Agent port for UDP/TCP connections
# Default: 2019
AGENT_PORT=2019

# Agent IP address to bind to
# 0.0.0.0 = listen on all interfaces
# Default: 0.0.0.0
AGENT_IP=0.0.0.0

# ==========================================
# SERIAL CONFIGURATION (for serial mode)
# ==========================================

# Serial device path
# Common devices:
#   /dev/ttyUSB0 - USB serial adapter
#   /dev/ttyACM0 - USB CDC (most ArduPilot)
#   /dev/ttyAMA0 - Raspberry Pi UART
# Default: /dev/ttyUSB0
SERIAL_DEVICE=/dev/ttyUSB0

# Serial baud rate
# Common rates:
#   115200  - Standard speed (reliable)
#   230400  - Medium speed
#   460800  - High speed
#   921600  - Very high speed (recommended)
#   1500000 - Maximum speed (some hardware)
# Default: 921600
BAUD_RATE=921600

# ==========================================
# DEBUGGING
# ==========================================

# Verbose logging level (0-6)
# 0 = Normal (default)
# 6 = Maximum verbosity (debugging)
# Default: 0
VERBOSE_LEVEL=0

# ==========================================
# COMMON CONFIGURATIONS
# ==========================================

# Configuration 1: SITL Testing (UDP)
# CONNECTION_MODE=udp4
# AGENT_PORT=2019
# GROUND_STATION_IP=192.168.1.100
#
# ArduPilot SITL command:
#   sim_vehicle.py --console --map -A "--serial3=udp:127.0.0.1:2019"

# Configuration 2: Real Hardware (Serial)
# CONNECTION_MODE=serial
# SERIAL_DEVICE=/dev/ttyUSB0
# BAUD_RATE=921600
#
# Remember to:
#   1. Uncomment 'devices' and 'privileged' in docker-compose.companion.yml
#   2. Set ArduPilot parameters:
#      SERIAL3_PROTOCOL = 45
#      SERIAL3_BAUD = 921600
#      DDS_ENABLE = 1

# Configuration 3: Network Testing (TCP)
# CONNECTION_MODE=tcp4
# AGENT_PORT=2019
# GROUND_STATION_IP=192.168.1.100
#
# ArduPilot configuration:
#   DDS_ENABLE = 1
#   DDS_PROTOCOL = 1
#   DDS_PORT = 2019
