# ROS 2 Gazebo Desktop - VNC Configuration
# ==========================================
# Use this configuration for remote access via VNC
# Much better performance than X11 forwarding

# VNC Server Configuration
# ========================
# Password for VNC connection (change this!)
VNC_PASSWORD=rospassword

# Screen resolution (adjust based on your needs)
# Common options: 1920x1080, 1680x1050, 1440x900, 1280x720
VNC_RESOLUTION=1920x1080

# Color depth (16, 24, or 32)
VNC_DEPTH=24

# User Configuration
# ==================
USER_UID=1000
USER_GID=1000
USERNAME=rosuser

# ROS 2 Configuration
# ===================
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# Container Configuration
# =======================
CONTAINER_NAME=ros2_dev_station

# Workspace Configuration
# ========================
# Built-in workspace (DO NOT change - contains micro-ROS agent, px4_msgs)
# Development workspace for your custom packages
DEV_WORKSPACE=./dev_ws

# PX4 Integration
# ===============
# Path to PX4 Gazebo models/worlds (mounted from px4-sitl-container)
PX4_GAZEBO_PATH=./px4-sitl-container/PX4-Autopilot/Tools/simulation/gz

# Gazebo Configuration
# ====================
# GZ_SIM_RESOURCE_PATH points to PX4 models/worlds (set in docker-compose)
GZ_PARTITION=gazebo

# Graphics Rendering Configuration
# ==================================
# These settings work TOGETHER for optimal software rendering in VNC:
#
# How it works:
# 1. EGL_PLATFORM=surfaceless    : Headless EGL for VNC (set in docker-compose)
# 2. EGL_DRIVER=swrast           : Software rasterization interface (set in docker-compose)
# 3. GALLIUM_DRIVER=llvmpipe     : LLVM-optimized rendering engine (set in docker-compose)
# 4. GZ_SIM_RENDER_ENGINE_NAME   : Ogre renderer (better for software, set in docker-compose)
#
# swrast and llvmpipe work TOGETHER:
# - swrast = interface (tells EGL to use software)
# - llvmpipe = implementation (the actual fast renderer)
#
# LIBGL_ALWAYS_SOFTWARE:
#   0 = Let EGL handle rendering mode (recommended with EGL_DRIVER=swrast)
#   1 = Force OpenGL software rendering (may conflict with hardware selection)
LIBGL_ALWAYS_SOFTWARE=0

# Network Ports (change if conflicts)
# ====================================
# VNC_PORT=5901       # Main VNC port
# NOVNC_PORT=6080     # Web browser access port

# Performance Settings
# ====================
# Memory limit for container
MEMORY_LIMIT=8G
MEMORY_RESERVATION=4G

# Shared memory size for Gazebo
SHM_SIZE=2gb
